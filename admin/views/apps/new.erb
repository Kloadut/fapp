<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:apps, :index) %></li>
      <li class="active"><%= link_to pat(:new), url(:apps, :new) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:new) %> <% mt(:app) %></h2>
    <div class="inner">
      <% form_for :app, url(:apps, :create), :class => :form do |f| %>
        <%= partial "apps/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>
